/**
 * VOZ DE NICO CON ELEVENLABS (opcional)
 *
 * Copia este archivo como  js/data/voz.config.js  y pega tu clave.
 * Si el archivo voz.config.js no existe, Nico habla con la voz del navegador,
 * que funciona sin internet y sin gastar créditos.
 *
 * IMPORTANTE: la clave va dentro del navegador, así que cualquiera que abra
 * la aplicación puede verla y gastar tus créditos. Por eso voz.config.js está
 * en .gitignore y NO se sube al repositorio. Úsalo solo para la demostración.
 *
 * La clave buena empieza por "sk_" y solo se ve una vez, al crearla en
 * https://elevenlabs.io/app/settings/api-keys
 * (el identificador que aparece en la lista de claves NO sirve).
 */
export const VOZ_ELEVENLABS = {
    apiKey: "sk_pega_aqui_tu_clave",
    voiceId: "xf3Xv0R9rgFTExG0MVNo",
    modelId: "eleven_multilingual_v2",
    velocidad: 0.9
};
